import js from '@eslint/js';
import globals from 'globals';
import reactHooks from 'eslint-plugin-react-hooks';
import reactRefresh from 'eslint-plugin-react-refresh';
import tseslint from 'typescript-eslint';

// ESLint 9 flat config. typescript-eslint v8 + eslint-plugin-react-hooks v7 +
// eslint-plugin-react-refresh both ship flat-config-compatible exports.
// Verify plugin rule-object shapes against the installed versions on first
// `npm install` — plugin APIs occasionally shift between majors.
export default tseslint.config(
  { ignores: ['dist', 'node_modules'] },
  {
    files: ['**/*.{ts,tsx}'],
    extends: [js.configs.recommended, ...tseslint.configs.recommended],
    languageOptions: {
      ecmaVersion: 2022,
      globals: globals.browser,
    },
    plugins: {
      'react-hooks': reactHooks,
      'react-refresh': reactRefresh,
    },
    rules: {
      ...reactHooks.configs.recommended.rules,
      'react-refresh/only-export-components': [
        'warn',
        { allowConstantExport: true },
      ],
      // Feature stubs intentionally throw on missing env vars, etc.; keep
      // unused-var checking but let underscore-prefixed args pass (common
      // in stub handlers awaiting the real implementation).
      '@typescript-eslint/no-unused-vars': [
        'warn',
        { argsIgnorePattern: '^_', varsIgnorePattern: '^_' },
      ],
    },
  },
);
